<h1>New Task</h1>

<%= form_for(@task) do |t| %>
  <p>
    <%= t.label :title %>
    <%= t.text_field :title %>
  </p>
  <p>
    <%= t.label :details %>
    <%= t.text_field :details %>
  </p>
  <%= t.submit %>
<% end %>

<%= link_to "Back to list", tasks_path %>
